# ─────────────────────────────────────────────
# TransactIQ Backend — Environment Variables
# Copy this file to .env and fill in your keys
# NEVER commit .env to GitHub
# ─────────────────────────────────────────────

# SERVER
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# ─── JWT SECRET (change this to a long random string) ───
JWT_SECRET=transactiq_super_secret_jwt_key_change_this_2024
JWT_EXPIRES_IN=30d

# ─── MySQL DATABASE ───
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=transactiq_db

# ─── FAST2SMS (Mobile OTP) ───
# Sign up free at https://www.fast2sms.com
# Dashboard → Dev API → API Key
FAST2SMS_API_KEY=your_fast2sms_api_key_here

# ─── GMAIL SMTP (Email OTP) ───
# Use your Gmail address
# IMPORTANT: Enable 2-Step Verification on Gmail first
# Then go to: Google Account → Security → App Passwords
# Create an App Password for "Mail" → copy the 16-char password below
GMAIL_USER=yourgmail@gmail.com
GMAIL_APP_PASSWORD=xxxx xxxx xxxx xxxx

# ─── OTP CONFIG ───
OTP_EXPIRY_MINUTES=10
OTP_LENGTH=6
MAX_OTP_ATTEMPTS=5

# ─── RATE LIMITING ───
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
OTP_RATE_LIMIT_MAX=5
